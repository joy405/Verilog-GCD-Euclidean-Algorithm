# Verilog-GCD-Euclidean-Algorithm
Designed and implemented a 16-bit GCD (Greatest Common Divisor) processor in Verilog HDL using the Euclidean subtraction algorithm. The project uses FSM-based control logic with separate datapath and controller architecture, including registers, multiplexers, comparator, and subtractor modules.
